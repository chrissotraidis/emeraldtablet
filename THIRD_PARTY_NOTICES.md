# Third-party notices

This wrapper currently vendors no extra libraries of its own.

The pinned Akhenaten engine and the dependencies it fetches at build time
remain under their upstream licenses. A complete dependency manifest with
versions and hashes will be recorded before any release candidate in G9.

Do not treat this file as complete until that audit exists.
